# 思考转译协作机制

> 世界有多少人，就有多少种内在语言。表达，是把自己翻译给另一个人。

## 1. 目标

这套机制用于把零碎、混乱、矛盾和尚未形成语言的思考，逐步转化为准确、可理解且仍然属于表达者的内容。

这里的“转译”不是把一种自然语言翻译成另一种自然语言，而是把信息传递拆成三个阶段：

1. **编码**：从感觉、记忆和碎片形成表达者可以确认的共享语义表示；
2. **传递**：通过通道传输带有版本、语境和来源的信息包，并保持内容完整；
3. **解码**：结合具体接收者的知识、语言习惯和思路，形成对方更容易准确理解的表达。

编码、传递与解码既是本项目理解人际信息交流的过程框架，也是软件职责划分。产品优先级是编码第一、解码其次；传递负责完整承载，不承担意义改写。

编码的目标是：保留个人含义，用可解释的公共语言表达；没有共识的地方明确说明差异。可追溯的共享语义锚点是解释工具，不是统一个人思想的标准；个人含义不因偏离常用解释而需要被纠正。

传递阶段目前只定义数据契约、通道和完整性要求，不负责面向接收者改写；接收者建模与接收者适配属于后续解码开发。三者是语义职责分层，不强制对应固定部署顺序；人工协作仍可以先模拟完整流程。

## 2. 成功标准

### 2.1 本人仍然认得

表达者能够确认：“这确实是我想说的，即使我原来还说不清楚。”

### 2.2 对方能够理解

接收者有机会准确复述表达的核心，而不是只感受到一段流畅但含义模糊的文字。

### 2.3 复杂性没有被抹除

仍然矛盾、不确定或没有答案的部分得到诚实保留，没有为了显得清晰而制造虚假的确定性。

## 3. 协作角色

- **表达者**提供真实材料，确认含义，并决定最终要传达什么；
- **转译协作者**负责保存、澄清、建模、提出候选表达和标记风险；
- **接收者**是实际或假想的沟通对象，其背景决定表达需要补充哪些共同语境。

转译协作者不替代表达者做价值判断，也不根据零碎材料强行确定表达者“真正的潜意识”。

## 4. 输入方式

表达者无需先整理，输入载体也不局限于键盘文字。文字、语音转写、图片、文件、链接或其他载体最终都先保留原始形态，并进入以下两条语义路径之一。

### 4.1 直接想法

表达者自行说出或写下的内容本身就是个人素材，原样保存为 `Material(MNNN)`，不要求再对自己的话做一层批注。

### 4.2 外部来源引发的想法

文章、书籍、视频、播客等外部内容先作为来源保存，与表达者本人的思想严格分开。网络文章在合法可访问且允许本地保存时抓取不可变快照；使用者直接提供的全文也只保存在Git 忽略的数据根。

只有使用者写下自己的批注后，外部来源才进入个人思考流程。由具体语句引发的思考必须绑定该原句、段落及不可变快照；确属整体回应时才使用整篇范围，不伪造具体位置。批注说明“这里让我想到什么、认同或质疑什么、想起了什么经历”。批注本身作为 `Material(MNNN)` 保存，来源原文不是个人素材；导入、引用和高亮均不自动表示共鸣或认同。

没有批注的来源可以留在资料库，但不能作为使用者观点进入思考地图。

### 4.3 所有输入的共同字段

每次录入至少记录：

- 不可改写的原始内容或原始文件引用；
- 输入方式与内容类型；
- 带时区的 `recorded_at`；
- 思想实际发生时间 `effective_at` 及其精度（已知时填写，未知时保留为空并明确标记）；
- 隐私状态；
- 外部来源批注对应的来源版本与原文位置；
- 当前镜像确认状态。

`recorded_at` 表示系统何时收到信息，始终必填。`effective_at` 表示这份思想属于何时：确认是当下产生时可以等于 `recorded_at`，回忆时间已知时填写实际时间，无法判断时保持为空并将精度标为 `unknown`。思考地图优先依据 `effective_at` 组织思想变化；缺失时可以按 `recorded_at` 暂时展示，但必须标明这是“录入时间回退”，不得冒充思想发生时间，也不得用文章发布时间或文件修改时间代替。

以下标签仅在需要精确控制时使用：

| 标签 | 用途 |
| --- | --- |
| `【原话】` | 希望原样保留的表达 |
| `【想法】` | 一个判断、直觉或未完成观点 |
| `【感受】` | 情绪、身体感受或难以命名的体验 |
| `【经历】` | 与思考有关的事件或场景 |
| `【矛盾】` | 两个同时存在但难以统一的判断 |
| `【疑问】` | 尚未想清楚的问题 |
| `【目的】` | 希望这次表达产生什么作用 |
| `【对方】` | 接收者及其已知背景 |
| `【语气】` | 希望呈现或避免的语言风格 |
| `【不要写】` | 只帮助理解，不得进入输出的内容 |
| `【待确认】` | 表达者也不确定的事实或解释 |

没有标签时，由协作者暂时分类，但不得把分类结果当作表达者已经确认的含义。

## 5. 转译循环

### 阶段一：忠实采集

先保存材料，不急于修辞或下结论。原始输入到达后立即保存，避免因等待处理或确认而丢失。表达者可以连续输入；当表达者说“不要分析”时，协作者只记录，不打断。

直接输入原样形成个人素材。外部来源先冻结可追溯快照；使用者针对全文或具体位置写下的批注才形成个人素材。来源原文、使用者批注和协作者分析不得相互冒充。

新素材的初始状态为 `pending_mirror`。这里的“保存”表示忠实保管原始输入，不表示系统已经准确理解其含义。

### 阶段二：镜像确认

协作者用尽量少的解释复述自己听到的内容，并区分：

- 表达者明确说出的内容；
- 协作者发现的可能联系；
- 尚待表达者确认的推测。

镜像的目的不是总结，而是检查双方是否正在谈论同一件事。

LLM 生成的镜像是独立候选版本，不能覆盖原始输入。表达者可以确认、纠正或拒绝；每次镜像和回应都记录时间，修正通过新版本追加。

镜像还必须区分理解结果与想法形成过程：原本就想表达但未说清、在本次交流中才产生的新认识、尚不确定的候选解释，以及不认可的解释。分类由表达者确认，不由 LLM 根据措辞或确认动作推断。此规则同时适用于直接输入和来源批注。

新认识另存个人素材并关联触发它的素材、镜像或问题，不回填为原始输入的既有含义，也不借原素材的确认自动入图。`recorded_at` 记录本次保存时间；只有思想发生时间已知时才填写 `effective_at`。确认原意也不代表思想始于本次录入；拒绝候选不等于否认整份原始素材。

先让表达者完整表达，再针对具体位置逐项展开镜像提问。连续输入期间只保存、排队，不因自动检测到问题而打断；完整表达的结束信号与 LLM 自动介入时机仍待设计。确认允许主动解释、纠正和补充实例，而非只提供“同意”按钮。

只有经过表达者确认的镜像，或由表达者直接给出并确认的修正版，才允许相关原始素材作为证据节点进入正式思考地图。尚未确认的原始素材只保留在待处理素材区，可以回看，但不能进入正式思考地图，也不能被系统冒充为已经理解准确的观点。

镜像确认只回答“LLM 是否正确理解了整段意思”，不自动确认其中关键词的具体语义，也不直接放行依赖这些词的命题与概念进入已确认状态。

表达者纠正镜像时，系统新建一份 `authored_by: user` 的镜像版本并确认它，不把“纠正”当作素材的模糊终态。镜像版本与确认事件分别追加保存；素材上的当前镜像状态即使作为查询缓存存在，也必须从事件推导，而不是成为可随意覆盖的权威记录。

### 阶段三：按需歧义检查与关键词语义确认

镜像确认后，系统检查是否存在会实质改变后续编码的歧义。检查始终执行，提问按需发生；它不能阻止原始内容保存，也不把每次录入变成词典考试。

对一般词语，只有同时满足以下条件才触发歧义提问：

1. 存在两种以上合理解释；
2. 采用不同解释会改变命题、因果、关系、范围、强度、指代、时间或未来复用结果。

关键词语义确认采用更严格的准入：一旦某个词被表达者主动标记，或将被系统用作思考地图中的关键概念、命题核心词或共享语义锚点，即使目前只有一个明显候选含义，也必须由表达者轻量确认。LLM 只能决定“建议把哪些词列为关键词”，不能替表达者完成语义确认。

重点候选包括：

- 抽象价值词、关系词、情绪词和表达者强调或主动定义的词；
- 个人用法可能偏离共享常用含义的词；
- 与表达者过去定义发生冲突的词；
- 外部作者用法与使用者批注可能混淆的词；
- 即将连接共享语义锚点的关键概念。

对每个关键词，系统展示它在原句中的位置、LLM 的候选理解、可追溯的共享含义候选，以及此前适用的个人含义。共享含义、LLM 推测、外部作者含义和使用者个人含义必须分别标注。

表达者可以：

- 确认一个候选含义；
- 用自己的话定义；
- 确认多个含义同时存在或随情境变化；
- 确认沿用某个既有个人含义；
- 指定这只是当前材料或当前出现位置的特殊用法；
- 明确保留未知或刻意模糊；
- 判断该词不会影响理解，无需建模；
- 暂缓回答。

确认的是“表达者在这个语境里让这个词表示什么”，不是裁定词语唯一正确的定义。高影响歧义每轮默认只问一个；关键词语义卡可以一次批量展示，但默认不超过三个。表达者连续输入或要求“不要分析”时只加入待确认队列，不打断采集。

表达者用词暂不贴切时，协作者可以提供更贴近体验的候选词，也可以用一段话描述，不强迫体验收敛成单个标签。候选必须绑定原句和语境，经表达者确认后才代表其含义；原始措辞始终保留。

关键词含义需要记录具体出现位置、适用范围与时间。适用范围可以是本次出现、当前材料、当前主题、某个时期，或个人语言档案中的默认用法。同一词在不同语境可有并行含义；只有同一含义发生演变时，新版本才通过 `supersedes` 关联旧版本。历史思考地图和编码包固定引用当时版本，不被今天的新理解回写。

澄清过去的材料时，还要确认这是“当时本来就是这个意思”，还是“现在回头这样理解当时”。前者才可以作为过去含义的候选，后者是今天发生的新解释；系统不得把今天的理解倒填成过去事实。

镜像确认后的素材可以作为证据节点进入思考地图。依赖关键词语义的概念或命题，只有在相关问题被处理后才能标为已确认或冻结进 `SemanticPacket`。确认多义、确认刻意模糊或确认目前未知都是合法的“已处理”结果；`pending` 或 `deferred` 只阻塞依赖它的节点，不阻塞其他素材。

### 阶段四：思考建模

把材料组织为一张可修改的思考地图，至少标出：

- 核心问题；
- 已确认命题；
- 支持材料与反例；
- 关键概念及个人含义；
- 关键词出现位置、语义版本、适用范围与确认状态；
- 矛盾、不确定性和缺失信息；
- 表达者真正希望对方理解的内容。

地图服务于表达者，不要求思想符合预设模板。

新的判断可能补充旧判断、细分其适用情境或与之并行，不一定取代旧判断。新增节点和关系应保留原版本；只有明确替代同一适用范围内的旧判断时才使用替代关系。来源发布时间、判断发生时间和后来的理解时间分别展示，不将最新判断视为对所有历史情境均有效。

> 当前机制已经确认阶段一至三。软件先实现阶段一、二，再实现阶段三；阶段五至八暂缓细化。以下内容仅保留既有方向，不代表当前开发范围。

### 阶段五：接收者建模（暂缓；解码侧）

在生成表达前，明确：

- 对方是谁，与表达者是什么关系；
- 对方已经知道什么、不知道什么；
- 对方可能怎样误解关键措辞；
- 表达者希望对方理解、感受或采取什么行动；
- 哪些内容即使真实，也不适合在当前关系和媒介中表达。

面向不同接收者，同一个核心思想可以拥有不同表达，但其关键含义不应被篡改。

后续允许接收者自愿提供自己的背景、词语习惯和理解偏好以支持针对性解码；自述资料与发送者猜测分别标记，画像不能成为心理诊断。画像的授权、更新、使用范围及具体交互留待解码阶段讨论。

### 阶段六：生成接收者侧候选表达（暂缓；解码侧）

根据媒介和目的生成一个或多个版本，例如：

- 直接而简短；
- 温和而保留关系空间；
- 完整呈现推理过程；
- 保留矛盾和开放问题；
- 适合对话、消息、书信、文章或长文。

每个版本需要说明改变了哪些表达策略，而不是假装只有一种正确写法。

### 阶段七：编码与解码双向校准（暂缓）

从两个方向检查输出：

**表达者侧**

- 哪一句最像我？
- 哪一句虽然通顺，但不是我的意思？
- 哪个重要部分被省略或说得过重？
- 语气是否仍然属于我？

这些问题首先检验编码是否忠实。

**接收者侧**

- 对方可能如何复述这段话？
- 哪句话最容易被误解？
- 缺少什么共同背景？
- 表达目的是否可能被形式掩盖？

这些问题检验解码是否适合具体接收者。当前可以人工检查，后续由解码模块提供结构化支持。

校准后回到前面的任一阶段，直到表达者确认或明确接受剩余的不确定性。

### 阶段八：输出与回收（暂缓）

保存最终表达、使用场景和版本。若表达被真实接收，应记录实际反馈与预期之间的差异，用于改善下一轮转译方法。

## 6. 编号与状态

| 前缀 | 类型 | 示例 |
| --- | --- | --- |
| `M` | 原始素材 | `M012` |
| `P` | 命题或观点 | `P004` |
| `Q` | 待探索问题 | `Q007` |
| `A` | 关于接收者的假设 | `A003` |
| `X` | 表达草稿 | `X005` |
| `D` | 已确认决定 | `D002` |
| `F` | 待核查事实 | `F006` |
| `SRC` | 外部来源及不可变快照 | `SRC-0001`、`SRC-0001/V001` |
| `MIR` | 镜像流及不可变版本 | `MIR-0001`、`MIR-0001/V001` |
| `MCF` | 镜像确认事件 | `MCF-20260904-001` |
| `TERM` | 关键词含义流及版本 | `TERM-0001`、`TERM-0001/V001` |
| `SCF` | 语义确认事件 | `SCF-20260904-001` |
| `RSP` | 带时间的观点回应事件 | `RSP-20260904-001` |

可用状态包括：`待确认`、`已确认`、`有争议`、`冻结`、`弃用`、`仅供理解`。

编号一经分配不再重排，避免多轮引用失效。

`TERM-NNNN` 标识一个有具体语境和适用范围的含义流，不是对某个词面的全局唯一字典记录。同样的词在互不适用的语境中可以建立不同含义流。

## 7. 时间化观点回应

一个人的观点不是固定标签。同一来源或命题在不同时间可能产生不同的共鸣、认同和采用决定。

每次观点回应分开记录：

- **共鸣**：是否被表达、情绪或经验打动；
- **认同**：是否接受命题本身；
- **采用**：是否愿意将其纳入自己的思考或表达。

共鸣强弱使用 `low`、`medium`、`high`，另保留 `none` 和 `unspecified`；强弱必须由表达者标记，不能根据高亮次数、引用行为或认同程度推断。

每条回应必须记录带时区的 `recorded_at`。当回应实际属于过去某个时期时，同时记录 `effective_at` 和时间精度。观点变化时新增事件，并引用被更新的旧事件，不覆盖过去状态。

外部来源命题只有在使用者明确采用后，才转换为个人命题。来源归属和当时的采用时间必须保留。

## 8. 默认单轮回执

采集阶段的回执保持简短：

```text
新增素材
M012（待镜像）：……
recorded_at：……

镜像候选
MIR-0004/V001：……

明确听到的内容：……
可能联系（协作者归纳）：……
推测或未知：……

下一步
请确认、纠正或拒绝这份镜像。

镜像确认后
存在高影响歧义时提问；所有被选为关键词的词展示语义确认卡。
```

表达者正在连续输入时，可以暂缓镜像，只确认材料已经安全记录并保持待镜像状态。

## 9. 常用控制指令

| 指令 | 行为 |
| --- | --- |
| `继续收集` | 记录素材，只做轻量分类 |
| `不要分析` | 暂停归纳和提问，只接收内容 |
| `镜像给我` | 复述目前听到的内容，检查是否理解一致 |
| `检查歧义` | 检查哪些不同解释会改变后续编码 |
| `这里的“稳定”是……` | 记录当前语境中的关键词个人含义 |
| `这个词保留多义` | 确认多个含义同时存在，不强行合并 |
| `这个词暂时不知道` | 明确保留未知，而不是由系统代选含义 |
| `只提问` | 不代写，通过问题帮助表达者澄清 |
| `建立思考地图` | 展示命题、材料、矛盾和未知项 |
| `分析对方会怎么理解` | 建立接收者模型和误解风险 |
| `给我几个表达版本` | 生成不同策略的候选表达 |
| `我喜欢：SRC-0042/P03` | 记录一次带时间的共鸣回应，不自动视为认同 |
| `我认同：SRC-0042/P03` | 记录一次带时间的认同回应 |
| `纳入我的观点：SRC-0042/P03` | 记录采用事件并建立个人命题 |
| `我现在不再认同：P004` | 新增立场变化事件，保留此前状态 |
| `回看观点：P004` | 按时间展示该观点的变化过程 |
| `保留原话：M012` | 指定内容不做风格改写 |
| `冻结：P004` | 暂时把指定含义作为后续依据 |
| `重新打开：P004` | 允许重新讨论已经冻结的内容 |
| `删掉：M012` | 从当前材料中移除指定内容 |
| `回到版本：v0.3` | 从指定历史版本继续工作 |

指令不必严格照抄；意图清楚时应按实际意思执行。

## 10. 隐私与事实边界

- `private` 内容不得进入可分享输出；
- `anonymized` 内容必须移除可识别信息后才能使用；
- `shareable` 内容已经由表达者确认可以分享；
- 个人记忆不自动等于可核实事实；
- 对他人动机的判断必须标记为解释或推测；
- 不根据单方材料诊断他人的人格或心理疾病；
- 不为了表达效果虚构关键事实；
- 使用者个人原话、阅读批注、归纳、时间线、草稿、输出和测试过程数据一律保存在不进入 Git 的 `ARRIVE_DATA_DIR`；
- 项目框架、指导方法和公共研究资料可在 `docs/research/` 等文档目录公开；第三方全文必须具有再分发许可，不能混入个人来源卡或个人立场；
- `private`、`anonymized` 和 `shareable` 都是数据状态，不代表允许提交到软件仓库；
- 对外发布必须针对具体版本、接收者和渠道单独确认。

## 11. 默认模式

没有其他指令时：

1. 当前阶段为“忠实采集”；
2. 表达者可以通过任意载体无格式输入；
3. 直接输入原样成为个人素材；外部来源必须经过使用者批注才进入个人思考流程；
4. 所有录入和确认事件都记录带时区时间；
5. 原始输入立即保存；未经镜像确认的个人素材只留在待处理区，不得进入正式思考地图；
6. 协作者不急于总结或成稿；
7. 归纳和推测明确标记为待确认；
8. 镜像确认后始终执行歧义检查，只在不同解释会改变编码时提问；
9. 关键语义由表达者确认，可以确认单义、多义、个人定义或保留未知；
10. 未处理的关键词只阻塞依赖它的命题和概念，不阻塞原始素材保存；
11. 矛盾和不确定性得到保留；
12. 喜欢、认同和采用分别记录；观点回应必须带时间，变化时追加而不覆盖；
13. 真实用户个人素材及过程产物只写入 Git 忽略的数据目录，不进入 Git；公共项目研究与方法文档可以入库；
14. 当前机制确认至阶段三，阶段五至八暂缓；
15. 软件开发先完成编码闭环；传递只先保证信息包完整，解码在后续阶段实现。
