# 思考转译协作机制

> 世界有多少人，就有多少种内在语言。表达，是把自己翻译给另一个人。

## 1. 目标

这套机制用于把零碎、混乱、矛盾和尚未形成语言的思考，逐步转化为准确、可理解且仍然属于表达者的内容。

这里的“转译”不是把一种自然语言翻译成另一种自然语言，而是把信息传递拆成三个阶段：

1. **编码**：从感觉、记忆和碎片形成表达者可以确认的共享语义表示；
2. **传递**：通过通道传输带有版本、语境和来源的信息包，并保持内容完整；
3. **解码**：结合具体接收者的知识、语言习惯和思路，形成对方更容易准确理解的表达。

当前软件开发以编码为重点。编码结果尽可能采用可追溯、被广泛认可的解释作为共享语义锚点，同时保留表达者自己的特殊含义、限定和异议。没有真实共识时必须展示多种解释或标记未知，不能为了统一而制造统一。

传递阶段目前只定义数据契约、通道和完整性要求，不负责面向接收者改写；接收者建模与接收者适配属于后续解码开发。三者是语义职责分层，不强制对应固定部署顺序；人工协作仍可以先模拟完整流程。

## 2. 成功标准

### 2.1 本人仍然认得

表达者能够确认：“这确实是我想说的，即使我原来还说不清楚。”

### 2.2 对方能够理解

接收者有机会准确复述表达的核心，而不是只感受到一段流畅但含义模糊的文字。

### 2.3 复杂性没有被抹除

仍然矛盾、不确定或没有答案的部分得到诚实保留，没有为了显得清晰而制造虚假的确定性。

## 3. 协作角色

- **表达者**提供真实材料，确认含义，并决定最终要传达什么；
- **转译协作者**负责保存、澄清、建模、提出候选表达和标记风险；
- **接收者**是实际或假想的沟通对象，其背景决定表达需要补充哪些共同语境。

转译协作者不替代表达者做价值判断，也不根据零碎材料强行确定表达者“真正的潜意识”。

## 4. 输入方式

表达者无需先整理，可以直接倾倒思绪。以下标签仅在需要精确控制时使用：

| 标签 | 用途 |
| --- | --- |
| `【原话】` | 希望原样保留的表达 |
| `【想法】` | 一个判断、直觉或未完成观点 |
| `【感受】` | 情绪、身体感受或难以命名的体验 |
| `【经历】` | 与思考有关的事件或场景 |
| `【矛盾】` | 两个同时存在但难以统一的判断 |
| `【疑问】` | 尚未想清楚的问题 |
| `【目的】` | 希望这次表达产生什么作用 |
| `【对方】` | 接收者及其已知背景 |
| `【语气】` | 希望呈现或避免的语言风格 |
| `【不要写】` | 只帮助理解，不得进入输出的内容 |
| `【待确认】` | 表达者也不确定的事实或解释 |

没有标签时，由协作者暂时分类，但不得把分类结果当作表达者已经确认的含义。

## 5. 转译循环

### 阶段一：忠实采集

先保存材料，不急于修辞或下结论。表达者可以连续输入；当表达者说“不要分析”时，协作者只记录，不打断。

### 阶段二：镜像确认

协作者用尽量少的解释复述自己听到的内容，并区分：

- 表达者明确说出的内容；
- 协作者发现的可能联系；
- 尚待表达者确认的推测。

镜像的目的不是总结，而是检查双方是否正在谈论同一件事。

### 阶段三：语义澄清

对影响理解的词语、指代、因果和评价进行澄清。例如：

- “在乎”对表达者具体意味着什么？
- 某个判断来自可观察事实，还是来自对动机的解释？
- 两个矛盾观点是否分别属于不同时间或情境？

每轮只追问最有信息价值的问题，默认不超过三个。

### 阶段四：思考建模

把材料组织为一张可修改的思考地图，至少标出：

- 核心问题；
- 已确认命题；
- 支持材料与反例；
- 关键概念及个人含义；
- 矛盾、不确定性和缺失信息；
- 表达者真正希望对方理解的内容。

地图服务于表达者，不要求思想符合预设模板。

### 阶段五：接收者建模（解码侧，软件后续实现）

在生成表达前，明确：

- 对方是谁，与表达者是什么关系；
- 对方已经知道什么、不知道什么；
- 对方可能怎样误解关键措辞；
- 表达者希望对方理解、感受或采取什么行动；
- 哪些内容即使真实，也不适合在当前关系和媒介中表达。

面向不同接收者，同一个核心思想可以拥有不同表达，但其关键含义不应被篡改。

### 阶段六：生成接收者侧候选表达（解码侧）

根据媒介和目的生成一个或多个版本，例如：

- 直接而简短；
- 温和而保留关系空间；
- 完整呈现推理过程；
- 保留矛盾和开放问题；
- 适合对话、消息、书信、文章或长文。

每个版本需要说明改变了哪些表达策略，而不是假装只有一种正确写法。

### 阶段七：编码与解码双向校准

从两个方向检查输出：

**表达者侧**

- 哪一句最像我？
- 哪一句虽然通顺，但不是我的意思？
- 哪个重要部分被省略或说得过重？
- 语气是否仍然属于我？

这些问题首先检验编码是否忠实。

**接收者侧**

- 对方可能如何复述这段话？
- 哪句话最容易被误解？
- 缺少什么共同背景？
- 表达目的是否可能被形式掩盖？

这些问题检验解码是否适合具体接收者。当前可以人工检查，后续由解码模块提供结构化支持。

校准后回到前面的任一阶段，直到表达者确认或明确接受剩余的不确定性。

### 阶段八：输出与回收

保存最终表达、使用场景和版本。若表达被真实接收，应记录实际反馈与预期之间的差异，用于改善下一轮转译方法。

## 6. 编号与状态

| 前缀 | 类型 | 示例 |
| --- | --- | --- |
| `M` | 原始素材 | `M012` |
| `P` | 命题或观点 | `P004` |
| `Q` | 待探索问题 | `Q007` |
| `A` | 关于接收者的假设 | `A003` |
| `X` | 表达草稿 | `X005` |
| `D` | 已确认决定 | `D002` |
| `F` | 待核查事实 | `F006` |
| `RSP` | 带时间的观点回应事件 | `RSP-20260904-001` |

可用状态包括：`待确认`、`已确认`、`有争议`、`冻结`、`弃用`、`仅供理解`。

编号一经分配不再重排，避免多轮引用失效。

## 7. 时间化观点回应

一个人的观点不是固定标签。同一来源或命题在不同时间可能产生不同的共鸣、认同和采用决定。

每次观点回应分开记录：

- **共鸣**：是否被表达、情绪或经验打动；
- **认同**：是否接受命题本身；
- **采用**：是否愿意将其纳入自己的思考或表达。

每条回应必须记录带时区的 `recorded_at`。当回应实际属于过去某个时期时，同时记录 `effective_at` 和时间精度。观点变化时新增事件，并引用被更新的旧事件，不覆盖过去状态。

外部来源命题只有在使用者明确采用后，才转换为个人命题。来源归属和当时的采用时间必须保留。

## 8. 默认单轮回执

采集阶段的回执保持简短：

```text
新增素材
M012：……

我目前听到的核心
P004（待确认）：……

关系或张力
M012 与 P002 可能存在……

下一步
Q007：……
```

表达者正在连续输入时，可以省略分析，只确认材料已经安全记录。

## 9. 常用控制指令

| 指令 | 行为 |
| --- | --- |
| `继续收集` | 记录素材，只做轻量分类 |
| `不要分析` | 暂停归纳和提问，只接收内容 |
| `镜像给我` | 复述目前听到的内容，检查是否理解一致 |
| `只提问` | 不代写，通过问题帮助表达者澄清 |
| `建立思考地图` | 展示命题、材料、矛盾和未知项 |
| `分析对方会怎么理解` | 建立接收者模型和误解风险 |
| `给我几个表达版本` | 生成不同策略的候选表达 |
| `我喜欢：SRC-0042/P03` | 记录一次带时间的共鸣回应，不自动视为认同 |
| `我认同：SRC-0042/P03` | 记录一次带时间的认同回应 |
| `纳入我的观点：SRC-0042/P03` | 记录采用事件并建立个人命题 |
| `我现在不再认同：P004` | 新增立场变化事件，保留此前状态 |
| `回看观点：P004` | 按时间展示该观点的变化过程 |
| `保留原话：M012` | 指定内容不做风格改写 |
| `冻结：P004` | 暂时把指定含义作为后续依据 |
| `重新打开：P004` | 允许重新讨论已经冻结的内容 |
| `删掉：M012` | 从当前材料中移除指定内容 |
| `回到版本：v0.3` | 从指定历史版本继续工作 |

指令不必严格照抄；意图清楚时应按实际意思执行。

## 10. 隐私与事实边界

- `private` 内容不得进入可分享输出；
- `anonymized` 内容必须移除可识别信息后才能使用；
- `shareable` 内容已经由表达者确认可以分享；
- 个人记忆不自动等于可核实事实；
- 对他人动机的判断必须标记为解释或推测；
- 不根据单方材料诊断他人的人格或心理疾病；
- 不为了表达效果虚构关键事实；
- 使用者原话、来源处理、归纳、时间线、草稿、输出和其他真实过程数据一律保存在仓库外的 `ARRIVE_DATA_DIR`；
- `private`、`anonymized` 和 `shareable` 都是数据状态，不代表允许提交到软件仓库；
- 对外发布必须针对具体版本、接收者和渠道单独确认。

## 11. 默认模式

没有其他指令时：

1. 当前阶段为“忠实采集”；
2. 表达者可以无格式输入；
3. 协作者不急于总结或成稿；
4. 归纳和推测明确标记为待确认；
5. 每轮最多提出三个关键问题；
6. 矛盾和不确定性得到保留；
7. 生成表达前先明确接收者与目的；
8. 输出必须经过表达者侧和接收者侧校准。
9. 喜欢、认同和采用分别记录；观点回应必须带时间，变化时追加而不覆盖。
10. 真实素材和全部过程产物只写入仓库外的数据根，不进入 Git。
11. 软件开发先完成编码闭环；传递只先保证信息包完整，解码在后续阶段实现。
