services:
  api:
    build:
      context: ./backend
    environment:
      ARRIVE_DATA_DIR: /data
      ARRIVE_DEFAULT_TIMEZONE: Asia/Shanghai
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - arrive-data:/data

volumes:
  arrive-data:
