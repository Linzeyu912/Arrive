# Repository instructions

本仓库是“抵达 / Arrive”：一个把混乱内在思考转译为忠实、可理解表达的长期项目。

处理本仓库内容时遵守以下规则：

1. 开始整理前先阅读 `思考转译协作机制.md`；处理长文时同时阅读 `长文写作协作机制.md`。
2. 默认处于“采集”阶段。除非表达者明确要求，不要把新素材直接总结成确定观点或成稿。
3. 区分表达者原话、协作者归纳、协作者推测和待验证事实，不得相互冒充。
4. 澄清而不替代：协作者可以提出候选解释，但最终含义由表达者确认。
5. 不擅自消除矛盾。矛盾、不确定和开放问题可能是思想的重要组成部分。
6. 生成表达前，应明确接收者、表达目的、媒介和可能产生的误解；不清楚时标记为待确认。
7. 表达需要进行双向校准：表达者是否仍然认得，以及接收者能否准确理解。
8. 保持素材编号稳定。新增原始素材使用 `MNNN`，命题使用 `PNNN`，问题使用 `QNNN`，接收者假设使用 `ANNN`，表达草稿使用 `XNNN`，决定使用 `DNNN`；镜像流使用 `MIR-NNNN`，镜像版本使用 `MIR-NNNN/VNNN`，镜像确认事件使用 `MCF-YYYYMMDD-NNN`；关键词含义流使用 `TERM-NNNN`，含义版本使用 `TERM-NNNN/VNNN`，语义确认事件使用 `SCF-YYYYMMDD-NNN`。
9. 尊重每份材料的 `privacy` 字段。`private` 内容不得进入可分享输出，`anonymized` 内容必须去除可识别信息。
10. 默认使用中文，并保留表达者有辨识度的用词、节奏和语气，不把文本改造成统一模板语言。
11. 不依据单方素材诊断他人的心理、人格或动机，也不把推测写成事实。
12. 不提交密钥、令牌、证件、精确住址等高敏感信息。
13. 个人外部来源使用稳定编号 `SRC-NNNN`、不可变快照 `SRC-NNNN/VNNN`，保存到 Git 忽略的数据目录。公共项目研究使用独立的 `REF-NNNN`，可以在 `docs/research/` 保存书目、原创摘要、指导方法与许可允许再分发的副本；不复制个人来源卡或立场。
14. 第三方来源不得未经许可整篇复制入库。保留可追溯链接、自己的详细摘要和必要短摘；作者观点与使用者认同必须分别标记。
15. 抓取失败时记录实际状态，不猜测标题、作者或内容；后续取得可靠材料后再补全同一来源记录。
16. 用户作为个人素材提供的第三方全文只保存在 `ARRIVE_DATA_DIR/raw/sources/`，来源卡记录相对键、获得方式、字节数与 SHA-256。用于公共项目研究的全文另查再分发许可；仅公开可读不能自动上传全文。个人批注与测试数据始终不随文献发布。
17. 来源命题使用 `SRC-NNNN/PNN`；只有使用者明确确认后，才能转换为其个人命题 `PNNN`，并保留归属和影响关系。
18. 外部观点进入输出时必须保留作者归属、原始链接和必要限定；作者未知时明确标记，不伪造署名。
19. 将“喜欢或共鸣”“认同”“采用”作为三个独立维度，不得根据其中一项推断另一项。
20. 所有观点回应使用 `RSP-YYYYMMDD-NNN` 事件并记录带时区的 `recorded_at`；涉及过去观点时同时记录 `effective_at` 和时间精度。
21. 观点改变时新增事件，不覆盖或删除旧立场；明确替代同一适用范围内旧判断时才通过 `supersedes` 关联。补充、情境细分与并行观点保留各自范围，不默认使旧判断失效。当前观点必须标注 `as_of`。
22. 来源卡中的立场仅为当前摘要，数据根 `responses/` 的追加式时间线是使用者观点历史的权威记录。
23. 后端代码位于 `backend/`。观点回应事件保持追加式，不新增会覆盖或删除历史事件的 API。
24. 后端接收的事件时间必须包含时区，并同时保存原始 ISO 8601 表示和用于查询的 UTC epoch milliseconds。
25. 修改数据库模型时同步生成或更新 Alembic 迁移；修改领域行为时补充相应测试。
26. 当前后端没有用户认证和字段级加密，只能用于本地或受控环境，不得把它描述为可直接公开部署的私人数据服务。
27. 严格遵守 `docs/软件与数据隔离规范.md`：Git 仓库可保存软件、框架、指导方法、公共研究资料、空白模板和完全虚构的测试夹具；真实用户个人数据（包括开发者测试数据）及其派生、中间、最终产物都只保存在不进入 Git 的 `ARRIVE_DATA_DIR`。密钥与运行数据库仍禁止提交。
28. `privacy` 或 `shareable` 状态不授予 Git 提交权限。发布真实内容必须经过针对具体版本、接收者和渠道的独立明确确认。
29. Emotion 是项目总目录，数据默认放在其下的 `arrive-data/`，由根 `.gitignore` 整体排除；也允许外部数据根。数据根不得等于或包含项目根，不得落入源码目录，亦不得通过链接逃逸或连接回源码。SQLite 必须位于数据根的 `database/` 中；测试数据库使用系统临时目录。
30. 新增提交前运行 `python scripts/check_data_boundary.py`。若检查失败，先迁移数据，不得用强制添加或关闭检查绕过边界。
31. 真实来源、观点或经历不得作为测试夹具；测试内容必须明确为合成数据，不能由真实材料仅改名而来。
32. 忠实采集接受任意载体，但语义上分为直接输入与来源批注两条路径。直接输入原样成为个人素材；外部来源只有在使用者写下批注后，才产生可进入个人思考流程的 `MNNN`，导入行为本身不表示认同。
33. 每条来源导入、个人素材、批注、镜像和确认记录都必须有对应的带时区事件时间；公共审计字段使用 `recorded_at`，访问、抓取等领域事件另记 `accessed_at`、`captured_at`。个人思想的 `effective_at` 仅在已知或确认是当下时填写；未知时保留为空并标记时间精度，思考地图不得将来源发布时间、文件时间或未标注的录入时间冒充个人思想时间。
34. 原始输入立即保存为待镜像状态；LLM 镜像必须与原话分开、区分明确内容与推测，并允许使用者确认、纠正或拒绝。未经镜像确认的个人素材只能留在待处理区，不得进入正式思考地图。用户纠正产生新的用户署名镜像版本；镜像版本与确认事件均追加保存，不覆盖原始输入或旧镜像。
35. 阶段三已经确认为“按需歧义检查与关键词语义确认”：镜像确认后始终检查歧义；普通词只有不同解释会实质改变命题、关系、范围、强度、指代、时间或复用结果时才提问。高影响歧义每轮默认一个，连续采集时不得打断。
36. 镜像确认不等于关键词语义确认。被使用者主动标记或将作为关键概念、命题核心词、共享语义锚点的词，即使候选含义看似单一，也必须由使用者确认；LLM 只能提出关键词候选。镜像确认后，素材可以作为证据进入正式思考地图；依赖关键语义的概念或命题只有在相关问题被处理后才能标为已确认或冻结进 `SemanticPacket`。
37. 关键词确认允许单义、个人定义、多义并存、依情境变化、沿用旧义、当前特例、确认未知、不需建模或延期。确认多义、刻意模糊或目前未知是合法的已处理结果；只有 `pending` 或 `deferred` 阻塞依赖节点，且不得阻塞原始素材保存。
38. 关键词含义必须绑定具体出现位置、语境、适用范围和时间。共享语义候选、LLM 推测、外部作者含义与使用者个人含义分别保存；同词不同语境可并行，同一含义的变化通过追加版本和 `supersedes` 表达，不回写历史地图或编码包。
39. 真实歧义问题、关键词出现、个人含义版本、语义确认事件和共享锚点使用记录都属于使用者过程数据，只保存到不进入 Git 的 `ARRIVE_DATA_DIR`。当前优先依次实现忠实采集、镜像确认和阶段三；协作机制阶段五至八等待使用者逐项讨论。
40. 编码优先、解码其次。保留个人含义，用可解释的公共语言表达；没有共识时明确差异，不以公共定义纠正个人含义。
41. 镜像回应区分原意澄清、交流中新认识、尚不确定和拒绝解释，由表达者确认；新认识另存并关联触发记录，不倒填为过去原意。
42. 连续表达时延迟反馈，完整表达后针对具体位置逐项提问；LLM 的自动介入时机仍待讨论。可提供候选词或描述段落帮助表达体验，但不覆盖原话或代替确认。
43. 来源高亮、导入和引用不表示共鸣或认同；共鸣强弱由使用者选择，未说明时为 unspecified。具体原句触发的思考必须绑定该原句及快照。
44. 开发任务开始时阅读 `docs/开发进度与模型交接.md`；结束时按前端、后端分别更新实际进度、验证结果、已知问题与下一步，并追加带时区的交接记录。问题使用稳定工程编号，已修复项保留修复依据；不得把未实现、未验证或仍有缺陷的能力标为完成，也不得将真实用户内容写入交接文档。
