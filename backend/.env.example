# 数据目录必须位于软件仓库之外。
ARRIVE_DATA_DIR=D:/arrive-data
ARRIVE_DEFAULT_TIMEZONE=Asia/Shanghai

# 可选。未设置时使用 ${ARRIVE_DATA_DIR}/database/arrive.db。
# ARRIVE_DATABASE_URL=sqlite:///D:/arrive-data/database/arrive.db
