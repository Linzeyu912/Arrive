# 默认使用项目内被 Git 忽略的 arrive-data/；也可显式指定外部数据根。
# 按自己的机器选择绝对路径，不要直接沿用维护者的磁盘路径。
# ARRIVE_DATA_DIR=C:/Users/your-name/ArriveData
# ARRIVE_DATA_DIR=/home/your-name/ArriveData
ARRIVE_DEFAULT_TIMEZONE=Asia/Shanghai

# 可选。未设置时使用 ${ARRIVE_DATA_DIR}/database/arrive.db。
# 建议不设置数据库 URL，让它随数据根自动生成。
# 本文件不会自动加载；可设置 shell 环境变量，或使用 uvicorn --env-file。
