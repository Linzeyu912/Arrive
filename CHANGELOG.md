# Changelog

## Unreleased

- 新增完整前后端 Docker Compose 封装：Nginx 静态页面与同源代理、健康检查、项目内数据目录挂载、镜像摘要及后端哈希锁文件；加入 Docker 验收脚本和 CI，容器运行尚待可用 Docker 引擎验证。

- 新增第一期前端：记录、素材、来源与观点页面接入现有 API；完成主流程联调，三处草稿丢失问题已记录待修。
- 新增按前端、后端划分的开发进度与模型交接文档，统一问题编号、验证依据和接手规则；清理临时前端指导文档引用。

- 明确 Emotion 为项目总目录，默认数据根改为其下被 Git 整体忽略的 `arrive-data/`；同步路径校验、提交检查和使用规范。

- 建表收敛到 Alembic 单一来源：应用启动改为程序化执行迁移并移除 create_all 引导，测试同样走迁移路径；
- 为 create_all 时期创建的既有数据库提供一次性自动 stamp 引导；Docker 镜像补入 migrations 目录；
- 后端技术优化（不改技术路线）：修复无时区 `as_of` 查询返回 500 的问题，改为参数校验阶段的 422；
- 统一数据库完整性错误为稳定 409 响应，回应每日编号并发冲突自动重试；
- 回应每日编号（RSP-YYYYMMDD）的日期基准统一按默认时区换算，不再随客户端提交的时区偏移漂移；
- 时间双写（ISO 字符串与 epoch 毫秒）由同一整秒时刻派生，消除两列亚秒不一致；
- 收敛素材、来源、命题、思考地图的编号分配样板，缩短数据库内占位编号以兼容强制字符串长度的数据库；
- 回应目标 ID 按精确格式校验，思考地图引用不存在的素材编号时拒绝创建；
- 移除手工序列化层，读取模型改为属性映射；SQLite 启用 WAL 与忙等待，补充目标加生效时间的复合索引及迁移；
- 版本号收敛到 `arrive.__version__` 单一来源；补充回应编号、快照三轴、错误路径等自动化测试。
- 建立 Python、FastAPI、SQLAlchemy 和 SQLite 后端 MVP；
- 实现素材、来源及来源命题、个人命题、观点回应和思考地图 API；
- 实现带时区的追加式回应历史与任意时间点立场快照；
- 增加 Alembic 初始迁移、Docker 本地入口和后端自动化测试；
- 建立外部来源分类、归属、权利状态和时间化观点回应协议；
- 规定来源命题与个人命题分开编号，立场变化追加而不覆盖；
- 新增外部来源与观点回应空白模板；
- 建立软件与数据物理隔离规范，将全部真实素材和过程产物迁移到仓库外；
- 后端新增数据根与 SQLite 路径强制校验，来源档案只接受数据根相对键；
- 增加 Git 忽略规则、本地提交钩子、CI 边界检查和 Docker 构建上下文隔离；
- 将自动化测试中的实例内容替换为完全虚构的合成夹具；
- 将产品链路明确为编码、传递和解码三个阶段，当前开发聚焦编码；
- 定义共享语义锚点与个人语义差异并存的编码原则；
- 重写开发进度和里程碑，后续依次建设编码闭环、传递层与接收者解码；
- 明确直接输入与外部来源定位批注两条忠实采集路径，所有录入保留时间；
- 将镜像确认设为个人素材作为证据进入正式思考地图前的门槛，并保留追加式修正；
- 将阶段三确认为按需歧义检查与关键词语义确认，允许单义、多义、个人定义和明确未知；
- 增加关键词出现、个人含义版本与语义确认事件的通用模板和规划；
- 阶段五至八继续暂缓细化；
- 记录当前无认证与字段加密的部署边界。

## v0.1.0 — 2026-09-04

- 将项目从“感情分析与自传式写作”重新定位为“思考转译与准确表达”；
- 确定项目名称“抵达 / Arrive”；
- 建立中英文项目介绍；
- 增加内在表示、语义表示和沟通表示三层模型；
- 增加接收者建模与双向校准；
- 将个人经历和长文调整为验证样本与输出模式；
- 重建内容目录和表达模板。
