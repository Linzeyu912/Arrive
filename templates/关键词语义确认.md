---
term_id: TERM-NNNN
revision_id: TERM-NNNN/VNNN
surface_form: ""
normalized_form: ""
language: zh-CN
occurrence_refs: []
recorded_at: YYYY-MM-DDTHH:mm:ss+08:00
effective_at: null
time_precision: unknown
temporal_perspective: unknown
authored_by: llm
scope: this_occurrence
shared_semantic_anchor_refs: []
resolution_status_cache: pending
supersedes_revision: null
privacy: private
---

# 关键词语义确认

## 出现位置

- 素材：`MNNN`
- 已确认镜像：`MIR-NNNN/VNNN`
- 原句与精确文本范围：
- 若来自来源批注：`SRC-NNNN/VNNN` 及来源位置

不得只按词面绑定含义；每次确认必须能够回到具体语境。

`TERM-NNNN` 表示一个有明确语境和范围的含义流，不是词面的全局字典编号；同一词在互不适用的语境中可以使用不同含义流。

## 为什么需要确认

说明这个词为何会成为关键概念，或替换成另一种合理解释后会怎样改变命题、关系、范围、强度、指代、时间或未来复用结果。关键词即使只有一个明显候选含义，也必须由使用者确认。

## 候选含义

### LLM 候选解释

这是模型推测，不是使用者已经确认的含义。

### 共享语义候选

每个候选分别记录定义、来源、领域、适用人群、共识状态、版本或哈希以及 `accessed_at`。没有真实共识时并列保存，不制造统一。

### 外部作者含义候选

仅在涉及外部来源时填写。作者在原文中的用法与使用者批注中的用法不得相互冒充。

### 已有个人含义

列出范围适用的既有 `TERM-NNNN/VNNN`，不能因为词面相同就静默沿用。

## 使用者确认

- 事件编号：`SCF-YYYYMMDD-NNN`
- 目标含义版本：`TERM-NNNN/VNNN`
- 结果：`confirmed_single | confirmed_multiple | current_exception | reuse_existing | accepted_unknown | not_material | deferred | rejected`
- 使用者自己的定义：
- 同时保留的多个含义：
- 适用范围：`this_occurrence | this_material | this_topic | time_period | personal_default`
- 适用语境或条件：
- `recorded_at`：`YYYY-MM-DDTHH:mm:ss+08:00`
- `effective_at`：`null`
- 时间精度：`unknown`
- 时间视角：`contemporaneous | retrospective | unknown`
- 取代的上一确认事件：`SCF-YYYYMMDD-NNN | null`
- 修正后创建的用户版本：`TERM-NNNN/VNNN | null`

确认事件和含义版本分别追加保存。`confirmed_single`、`confirmed_multiple`、`current_exception`、`reuse_existing`、`accepted_unknown` 与 `not_material` 都表示问题已经处理。`rejected` 只表示拒绝当前候选，问题随即回到 `pending`，直到出现新的可确认版本；`pending` 或 `deferred` 只阻塞依赖该语义的概念和命题。

处理过去材料时，必须区分“当时就是这个意思”与“现在这样理解当时”，不得把今天的回看倒填为过去的词义事实。

## 与共享含义的差异

记录 `same_as | narrows | broadens | combines | contrasts | unrelated | unknown`，只描述关系，不用共享解释覆盖个人含义。
