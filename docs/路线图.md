# 开发进度与后续规划

> 状态截至：2026-09-04

## 1. 产品主链路

Arrive 将信息传递拆成三个边界清晰的阶段：

```text
表达者的内在思考
        ↓
   编码 Encoding          ← 当前开发重点
        ↓
共享语义表示 Semantic Packet
        ↓
   传递 Transmission      ← 先定义数据契约与完整性
        ↓
   解码 Decoding          ← 编码稳定后开发
        ↓
接收者能够进入的表达与理解
```

- **编码**回答“表达者究竟想传递什么”；
- **传递**回答“这份含义如何在通道中保持完整、可追溯”；
- **解码**回答“怎样按照具体接收者的知识、语言和思路，使其准确理解”。

当前不把三个阶段同时做深。先让编码结果稳定、忠实且可验证，再开发接收者侧的解码能力。

这是语义职责的分层，不强制规定软件的部署顺序。如果解码运行在接收者一侧，可以先传递 `SemanticPacket` 再解码；如果由发送侧服务代为生成接收者适配文本，则先调用解码模块生成沟通表示，再通过物理通道发送。两种实现都必须保持解码结果可追溯到同一个编码版本。

## 2. 当前开发进度

| 模块 | 状态 | 已具备 | 主要缺口 |
| --- | --- | --- | --- |
| 数据与隐私基础 | 已完成首版 | 软件与数据物理隔离、Git/CI 防线、本地 SQLite | 加密、用户与工作空间权限 |
| 领域数据基础 | 已完成首版 | 素材、来源命题、个人命题、观点回应、思考地图 | 编码任务与编码版本模型 |
| 时间与归属 | 已完成首版 | 带时区事件、历史快照、来源观点与个人观点分离 | 未知思想时间、更多更正事件和完整审计链 |
| 忠实采集 | 机制已确认，后端部分具备 | 原始素材时间、来源身份、原文档案引用与数据边界校验 | 多载体导入、来源快照版本、定位批注与实际落盘 |
| 镜像确认 | 机制已确认，软件未实现 | 人工协作规则 | 镜像版本、确认事件、入图准入约束 |
| 按需歧义检查与关键词语义确认 | 机制已确认，软件未实现 | 普通歧义触发原则、关键词必确认与结果类型已定义 | 歧义问题、词义版本、确认事件与依赖准入 |
| 编码机制 | 软件未闭环 | 命题、来源归属和矛盾结构 | 共享语义锚点、编码结果契约、确认流程 |
| 传递机制 | 尚未开发 | 已有版本与来源概念可复用 | 信息包、完整性校验、媒介封装 |
| 解码机制 | 规划阶段 | 人工协作规范中已有接收者校准方法 | 接收者模型、适配策略、理解反馈 |
| 产品界面 | 尚未开发 | FastAPI 与 OpenAPI 可验证领域行为 | 采集、确认、对比和回看界面 |

当前后端是编码系统的数据基础，还不是自动编码器，也没有实现接收者解码。

## 3. 当前主线：编码闭环

### 3.1 当前纵向切片：忠实采集与镜像确认

两种输入最终统一形成带时间的个人素材：

```text
外部来源 → 冻结来源快照 → 使用者对全文或具体位置批注 ┐
                                                         ├→ Material(MNNN)
使用者直接输入 → 原样保存，无需额外批注               ┘
                                                              ↓
                                                    pending_mirror
                                                              ↓
                                             LLM 镜像候选 + 使用者确认/纠正
                                                              ↓
                                       eligible_as_evidence / semantic_check
```

所有原始输入立即保存，但“保存”与“理解确认”分开。镜像候选不能覆盖原话；未经确认的个人素材可以回看，但只留在待处理区，不得进入正式思考地图。

外部批注必须锚定不可变来源快照。锚点支持全文、章节、段落、句子、文本范围、页码、音视频时间段或画面区域，同时保存 `source_snapshot_id`、快照哈希与相应定位信息，避免来源更新后定位漂移。

通用契约允许文字、音频、图片、视频、文档和其他载体。首个软件纵向切片先完成文字直接输入与网络文章批注，再以相同模型增加转写、OCR 和其他载体适配器；路线图不会把尚未实现的适配器描述为现有能力。

每条导入、批注、直接输入、镜像和确认记录都有带时区的公共审计时间 `recorded_at`；访问和抓取另记 `accessed_at`、`captured_at`。个人思想所属时间使用可空的 `effective_at` 和时间精度表达：已知是当下时可等于录入时间，未知时保留为空。思考地图需要明确标记时间回退；文章发布时间不等于使用者产生想法的时间。

### 3.2 已确认机制：按需歧义检查与关键词语义确认

镜像确认后始终运行歧义检查。普通词只有在存在多种合理解释，且不同解释会改变命题、关系、范围、强度、指代、时间或后续复用结果时，才向使用者提问。一旦一个词被使用者主动标记，或将成为关键概念、命题核心词或共享语义锚点，即使只有一个明显候选含义，也必须由使用者确认，LLM 不能自动放行。

```text
已确认镜像
    ↓
高影响歧义与关键词扫描
    ├─ 无实质歧义且无关键词 → semantic_ready
    └─ 有关键歧义或关键词 → 展示原句、共享含义候选、个人旧义与 LLM 候选
                              ↓
                         使用者处理词义
                              ↓
               单义 / 多义 / 当前特例 / 沿用旧义 /
               确认未知 / 不需建模 / 暂缓
```

每轮默认只询问一个最高影响歧义；关键词语义卡可以批量展示，但默认最多三个。确认结果必须带具体出现位置、适用范围、时间与不可变版本；以前确认过的含义只有在适用范围相同且没有冲突时才复用。

准入分为两层：镜像确认后的素材可以作为证据节点进入正式思考地图；依赖关键词语义的概念或命题，只有相关问题已经处理，才能标为已确认或冻结进 `SemanticPacket`。确认多义、确认刻意模糊或确认目前未知也属于已处理；`pending` 和 `deferred` 只阻塞依赖节点。

当前软件仍先交付忠实采集与镜像确认，再实现本机制。协作机制阶段五至八继续暂缓细化。

### 3.3 编码结果契约

下一阶段首先定义稳定的 `SemanticPacket`（暂定名）。它至少包含：

- 原始素材引用及需要保留的原话；
- 表达者确认的命题；
- 关键概念的个人含义；
- 关键词出现位置与已确认的含义版本；
- 可追溯的共享语义锚点；
- 个人含义与共享含义之间的差异；
- 支持、因果、条件、冲突和例外关系；
- 事实、推测、未知项和置信状态；
- 表达目的、边界和不得进入输出的内容；
- 编码版本、确认状态和时间。

编码产物不直接针对某一个接收者措辞。它先形成相对稳定的共同语义层，供后续传递和多种解码方式复用。

### 3.4 共享语义锚点

“大家统一认可的解释”在工程上表示**尽可能使用可追溯的共享语义**，而不是宣称所有人只有一种解释。共享锚点可以来自：

- 标准、法律定义或权威术语；
- 有可靠来源支持的领域共识；
- 特定社群或专业语境中的稳定用法；
- 日常语言中的稳定常用含义；
- 对争议概念并列保存的多种主要解释。

这些类别不是固定优先级。系统先依据当前领域、语境、表达目的和适用人群选择候选，再比较来源可靠性、时效性和解释覆盖范围。法律定义只在相应法域和法律语境中具有特殊权重，不能自动覆盖日常或个人语境。

每个锚点记录定义、来源、适用范围、版本、获取时间和共识状态。表达者自己的特殊含义作为“个人语义差异”独立保存，不冒充共享锚点。找不到真实共识时必须明确标记，不用流畅措辞制造虚假统一。

### 3.5 编码循环

```text
原始输入
  → 分段与类型标注
  → 镜像确认
  → 按需歧义检查与关键词语义确认
  → 命题与关系建模
  → 连接共享语义锚点
  → 标出个人语义差异
  → 表达者校准
  → 冻结一个编码版本
```

模型只能提出候选编码。未经表达者确认的归纳、关系和概念映射不能冒充最终含义。

### 3.6 编码验收指标

- **忠实度**：表达者是否仍然认得；
- **覆盖度**：重要素材、矛盾和边界是否遗漏；
- **可追溯性**：每个命题和解释能否回到来源；
- **共享可解释性**：陌生读者能否依据语义锚点理解关键概念；
- **差异透明度**：个人用法与常用含义是否分开；
- **不确定性诚实度**：未知和争议是否被保留；
- **版本稳定性**：修改是否通过新版本发生，而非静默覆盖。

## 4. 里程碑

### 里程碑 0：领域与安全基础——已完成首版

- [x] 稳定编号、来源归属和时间化观点；
- [x] 追加式观点回应与任意时间快照；
- [x] 可保留矛盾的思考地图；
- [x] 软件与真实数据物理隔离；
- [x] 数据边界测试、本地提交检查和 CI。

### 里程碑 1：忠实采集与镜像确认——下一步

- [ ] 定义直接输入与来源批注两种采集模式，以及通用载体引用契约；
- [ ] 首先实现文字直接输入和网络文章批注；
- [ ] 建立带版本、哈希和抓取时间的来源快照；
- [ ] 建立全文、章节、段落和文本范围定位；
- [ ] 建立追加式镜像版本与确认、纠正、拒绝事件；
- [ ] 只允许经镜像确认的个人素材作为证据进入正式思考地图；
- [ ] 支持 `effective_at` 未知而不自动冒充录入时间；
- [ ] 增加 Alembic 迁移、领域校验和 API 测试。

### 里程碑 2：按需歧义检查与关键词语义确认

- [x] 确认与镜像确认的职责边界；
- [x] 确认“始终检查、按需提问、允许保留多义与未知”；
- [x] 确认被选为关键词的词即使只有一个明显候选含义，也必须由使用者确认；
- [ ] 建立歧义问题与关键词出现位置模型；
- [ ] 建立个人含义版本、共享语义锚点引用与语义确认事件；
- [ ] 建立依赖准入：未处理问题不能放行相关已确认命题；
- [ ] 支持按语境和时间复用或重新确认词义；
- [ ] 用少量真实但不入 Git 的样本评估收益与打断成本。

### 里程碑 3：编码数据模型

- [ ] 定义编码任务、语义单元、概念、关系和编码版本；
- [ ] 定义共享语义锚点及来源、范围和共识状态；
- [ ] 定义个人语义差异；
- [ ] 输出第一版 `SemanticPacket` JSON Schema。

### 里程碑 4：人工可完成的编码闭环

- [ ] 支持候选命题、概念和关系的逐项确认；
- [ ] 支持矛盾、未知项和禁止输出边界；
- [ ] 支持编码版本对比、冻结与重新打开；
- [ ] 用至少 10 次真实但不入 Git 的样本验证。

### 里程碑 5：模型辅助编码

- [ ] 接入可替换的模型适配层；
- [ ] 保存提示版本、模型版本、输入引用和候选结果；
- [ ] 按最终确认的规则提出澄清问题和共享语义候选；
- [ ] 评估模型是否篡改、遗漏或过度确定；
- [ ] 保证模型不能绕过表达者确认。

### 里程碑 6：传递层

- [ ] 将冻结的编码版本封装为可传递信息包；
- [ ] 保存版本、来源、语境、隐私和完整性哈希；
- [ ] 定义序列化、通道、送达与完整性边界，不在本层做接收者适配；
- [ ] 支持明确授权后的导出，不直接把私人数据发布到软件仓库。

### 里程碑 7：接收者解码——后续开发

- [ ] 建立基于明确资料和可校准假设的接收者模型；
- [ ] 记录接收者的已知背景、概念习惯和推理顺序；
- [ ] 将共享语义映射到接收者更容易进入的词汇、例子和结构；
- [ ] 生成不同接收者的解码候选版本；
- [ ] 通过复述测试、误解检测和真实反馈校准解码结果；
- [ ] 不依据单方素材诊断或臆测接收者心理。

### 里程碑 8：端到端反馈闭环

- [ ] 比较发送前预测与接收后的真实理解；
- [ ] 区分失真来自编码、传递还是解码；
- [ ] 用反馈改进语义锚点和策略，但不改写历史记录；
- [ ] 建立跨时间、跨接收者的表达回顾。

## 5. 近期开发顺序

1. 设计 `SourceSnapshot`、来源定位批注、镜像版本与确认事件；
2. 实现“直接输入/来源批注 → 原始保存 → 镜像 → 使用者确认”的后端纵向切片；
3. 增加时间、归属、不可覆盖和入图准入测试；
4. 实现“歧义扫描 → 关键词候选 → 使用者语义确认 → 依赖节点放行”；
5. 再定义完整 `SemanticPacket` 与编码版本；
6. 编码闭环稳定后进入传递层，解码层排在其后。
