# 开发进度与模型交接

更新于：2026-09-09T20:21:52+08:00

本文是不同模型接手开发时的共同入口，记录实际实现、验证证据、问题和下一步；[路线图](./路线图.md)保留长期方向，[CHANGELOG](../CHANGELOG.md)记录发布变化。它只保存软件开发信息与完全合成的复现步骤，不收录真实用户素材、个人立场或运行日志。

## 1. 接手前先看

1. 阅读根 `AGENTS.md`、`思考转译协作机制.md`、[软件与数据隔离规范](./软件与数据隔离规范.md)，再读本文。
2. 检查 `git status --short`、当前分支与最近提交，保留其他任务未提交的改动。代码和测试是实现依据，不能把规划或上次口头说明当作已实现能力。
3. 优先选择下方明确的问题或下一步，结束时更新对应前端/后端栏目、验证记录和交接历史。
4. 本文可以更新当前摘要，但旧交接记录不删除。问题编号保持稳定，解决后注明修复提交和回归用例，不复用编号。
5. 状态使用“已实现且已验证 / 已实现待验证 / 待修复 / 未实现 / 待讨论”；未运行的检查写“未验证”。推测根因与已复现现象分开标注。

Docker 封装已新增，Compose 静态检查通过，但本机引擎不可用，容器构建/联调待验证（BE-008）；运行说明见 [Docker 运行指南](./Docker运行指南.md)。

当前总体判断：**前端第一期主流程可用，但有三处已复现的草稿丢失问题；后端编码基础已具备，镜像确认和关键词语义闭环尚未实现。** 下一步先处理 FE-001 至 FE-003；后端随后继续忠实采集与镜像确认切片。

项目布局：`frontend/` 为界面，`backend/` 为领域服务，`arrive-data/` 为默认数据根并整体 Git 忽略；Emotion 是包含三者的总目录。外部数据根仍可通过 `ARRIVE_DATA_DIR` 指定。SQLite 在数据根 `database/` 内；测试使用系统临时数据根。

## 2. 前端进度

### 已完成

| 范围 | 实际行为与证据 | 状态 |
| --- | --- | --- |
| 基础工程 | 独立 React 18 + TypeScript + Vite 工程；四个导航入口；暖白、留白、侧边栏；桌面和手机布局 | 已实现且已验证 |
| 后端连接 | 相对 `/api/v1`、`/health` 经 Vite 代理到本地 FastAPI，保留路径前缀；没有第二套业务后端 | 已实现且已验证 |
| 文字记录 | 原字符串提交、成功回执、提交中锁定、中文输入法保护、网络失败保留输入、不自动重发 POST | 已实现；现有测试通过，连续输入离开保护见 FE-001 |
| 素材回看 | 全量列表、详情从列表恢复、刷新后仍可读；默认显示录入时间 | 已实现且已验证 |
| 来源登记 | 元信息和创建时的来源命题；作者明确表达/协作者归纳分开；不宣称抓取全文 | 已实现且已验证；草稿保护见 FE-002 |
| 观点回应 | 共鸣/认同/采用独立；追加历史；服务端快照；明确采用后可在同一请求创建个人命题 | 已实现且已验证；草稿保护见 FE-003 |
| 数据边界 | 无浏览器正文持久化、无模型服务；前端软件目录已进入 Git 检查白名单，构建及测试生成物忽略 | 已实现；数据边界检查通过 |

对应代码主要在 `frontend/src/features/`、`frontend/src/api/`、`frontend/src/lib/`。运行步骤见 [前端 README](../frontend/README.md)。第一期初始实现提交为 `5c99b43`。

前端容器新增多阶段构建：固定 Node 镜像 + npm ci，固定 Nginx 镜像提供静态页面、SPA 回退与 API 同源代理。本机前端构建通过；容器内构建与 Nginx 实际运行待验证。

### 已知问题

三个问题均在 2026-09-06 使用真实本地服务、系统临时数据库和完全合成内容复现。它们尚未修复，现有 27 项前端测试没有覆盖这些路径。

| 编号 | 优先级 / 状态 | 复现与影响 | 代码依据和建议下一步 | 验收条件 |
| --- | --- | --- | --- | --- |
| FE-001 | 高 / 待修复 | 记录页保存第一段成功 → 输入第二段但不保存 → 点击素材。没有提醒，第二段丢失 | `features/record/RecordPage.tsx`：`useBlocker` 排除了 `saved`，再次输入仍保留该状态。按当前草稿是否有改动判断离开保护，兼顾保存成功后的正常跳转 | 第二段离开前提醒；取消后正文仍在；确认离开才丢弃；保存后的空输入不误拦截 |
| FE-002 | 高 / 待修复 | 来源页打开登记 → 输入标题等 → 点击侧边栏其他页。未提交表单直接丢失 | `features/sources/SourcesPage.tsx` 仅在收起时确认，没有路由保护。检查 `SourceForm` 的 dirty 信号，统一处理路由和关闭/刷新保护 | 导航及收起都能保护修改；取消保持全部字段及来源命题；成功登记不误拦截 |
| FE-003 | 高 / 待修复 | 来源命题回应页输入原话 → 点击“按此时间汇总” → 等查询完成。原话变为空，未提交回应丢失 | `features/responses/ResponsePage.tsx` 查询时把页面切到 loading，卸载持有草稿的 `ResponseForm`。将查询加载与草稿生命周期分离，或把草稿提升到稳定父层 | 刷新摘要后正文、三轴、时间和采用选项都保留；切换目标时不能把旧草稿误提交给新目标 |

前端后续还需专项验证所有表单的站内离开、浏览器前进后退、手机抽屉键盘焦点和错误响应边界；没有专项测试的项目不能因主流程成功而标为完成。

### 尚未实施与依赖

- 来源定位批注、镜像版本/确认、关键词含义与确认：等待后端对应契约（BE-002）。
- 正式地图工作区：等待镜像与关键语义准入，不能直接利用现有地图创建 API 绕过规则。
- 模型对话、文章生成、会话管理、多载体上传与公共部署：不属于当前已完成能力。
- 视觉方向已经实现，手机和桌面截图已检查；不等于全部可访问性与所有宽度均验收。

## 3. 后端进度

### 已完成

| 范围 | 实际行为与证据 | 状态 |
| --- | --- | --- |
| 技术基础 | Python 3.12、FastAPI、Pydantic、SQLAlchemy 2、Alembic、默认 SQLite；保持模块化单体 | 已实现且已验证 |
| 领域 API | 素材、来源与来源命题、个人命题、追加观点事件、时间快照、基础思考地图 | 已实现且已验证，编码闭环尚未完成 |
| 不变量 | 稳定编号、来源归属、回应三轴独立、追加历史、带时区时间与 epoch 双写、目标存在性与错误处理 | 已有测试；未知时间等缺口见下表 |
| 数据目录调整 | 默认从项目旁目录改为项目内被 Git 忽略的 `arrive-data/`；源码目录不得成为数据根，SQLite 位置仍受校验 | 已实现且已验证 |
| 启动迁移 | `main.py` 启动调用 `run_migrations()`，测试同样走 Alembic；不再以 `create_all` 作为应用建表入口 | 已实现且已验证 |
| 旧库兼容 | 对无 Alembic 版本的旧库提供 stamp 后升级；测试覆盖新库、当前结构旧库、初始结构旧库三类 | 三个合成场景已验证，非任意数据库结构保证 |
| Docker 迁移目录 | Dockerfile 补入 migrations，供安装后的应用寻找迁移目录 | 已修改；本轮未进行 Docker 镜像构建验证 |
| 自动录入归一化 | 素材/来源录入同事务入队；文件接收、后台转换、持久化任务、Markdown 读取与中断恢复 | 自动 HTML、文字 PDF、中文扫描 PDF 全链路及合成回归通过；复杂文档保真仍待验证；不需要前端转换按钮 |

关键文件：`backend/src/arrive/api.py`、`schemas.py`、`services.py`、`config.py`、`database.py`、`backend/migrations/`。测试入口见 [后端 README](../backend/README.md)。新增迁移测试在 `backend/tests/test_migrations.py`。

数据目录的实际迁移已在本地完成并核对文件完整性；个人文件不进入本交接文档或提交。API 中结构化结果目前存于数据库，数据根中的分类目录不代表所有对应文件存储模块已实现。

后端容器新增 requirements.lock（固定直接/间接依赖及哈希）、固定基础镜像、健康检查及 `/data` 配置。Compose 改为项目内数据根绑定挂载，旧命名卷不自动迁移。业务接口和数据库 schema 本轮无变化。

### 已知缺口和限制

| 编号 | 状态 | 现状与影响 | 下一步 / 完成依据 |
| --- | --- | --- | --- |
| BE-001 | 待修复 | Material 与 Response 未传 `effective_at` 时回退到 `recorded_at`，无法可靠表达未知思想时间。MaterialRead 返回非空时间 | 在忠实采集切片中同步模型、迁移、schema 与测试；未知保持空值并记精度，前端相应更新 |
| BE-002 | 未实现 | 无来源不可变快照、定位批注、镜像版本/确认事件及关键语义版本。已有地图仅检查基础引用，没有正式确认准入 | 先完成直接输入/来源批注 → 原始保存 → 镜像 → 用户确认；再做阶段三。准入必须由后端测试保障 |
| BE-003 | 已知限制 | 当前快照按生效时间逐轴取非默认值，不按情境分支计算，也不是“当时系统已知”的双时间快照；默认轴值不会清空旧状态 | 当前 UI 标清计算口径；完整情境、替代及双时间查询设计另行明确后实现 |
| BE-004 | 未实现 | 创建接口无幂等键，网络超时后无法保证安全重复提交 | 前端继续保留输入并人工查询核对；如增加幂等契约，需测试并发、重复及结果重放，不改追加历史原则 |
| BE-005 | 已知限制 | 来源需 HTTP(S) 链接；无追加来源命题、素材单条 GET、地图列表等接口 | 仅在实际下一切片需要时扩充契约；前端不能用假链接或本地持久化补造 |
| BE-006 | 部分验证 | 旧库 bootstrap 根据表/索引识别迁移版本，不是完整结构比对；Docker 与安装包运行路径也未做本轮全场景验证 | 新增迁移前复核旧版本识别逻辑并扩充测试；镜像发布前完成构建/启动验证 |
| BE-007 | 未实现 / 部署限制 | 无用户认证、字段级加密；多人数据库环境尚未完整验收 | 只在本地或受控环境使用。公开部署与多人能力需要独立实施和验证 |
| BE-008 | 已实现待验证 | Compose 封装、Docker 验收脚本和 CI 已新增；本机 Docker 引擎管道不存在，`docker compose build` 无法开始 | 在可用 Docker 引擎上运行 `python scripts/docker_smoke.py`，验证镜像构建、迁移、代理、来源命题含斜杠路径及重建后持久化；完成前不能标容器验收通过 |

### 下一开发顺序

BE-010（本机已恢复，基础样本复测通过）：此前 SciPy `_pava_pybind` 被 Windows 代码完整性策略阻止；本次相同模块在原虚拟环境与精简环境均加载成功，未调整系统策略，具体恢复原因未确认。新增 `scripts/check_document_conversion.py`，通过正常后端接口完成文字 PDF 与中文扫描 PDF 的自动转换，验证具体文本、Markdown、页码定位及原件字节。默认 PyPdfium 后端保持不变；复杂表格/多栏保真与长期稳定性尚未专项验收。

1. 明确忠实采集切片的字段与时间契约，处理 BE-001，兼容已有数据。
2. 来源快照/定位、直接输入与来源批注、追加镜像版本与确认事件（BE-002）。
3. 镜像确认后素材入图准入，并补充不可覆盖、归属和时间测试。
4. 按需歧义检查与关键词语义确认；保留多义、未知、延期和具体语境。
5. 再进入完整编码版本与 SemanticPacket。阶段五至八、传递和解码不因已有前端而提前实现。

## 4. 验证记录与范围

最近一次完整本地验收：2026-09-06，针对当前前端第一期与后端待提交改动；本次新增交接文档没有修复 FE-001 至 FE-003。

| 检查 | 工作目录 / 命令 | 实际结果 |
| --- | --- | --- |
| 前端自动化 | `frontend/`：`npm test` | 4 个测试文件、27 项通过；存在 React Router 后续版本提示，不影响本次通过 |
| 类型与构建 | `frontend/`：`npm run build` | TypeScript 检查与 Vite 构建通过 |
| 后端测试 | `backend/`：`python -m pytest` | 51 项通过，包含 3 项迁移测试 |
| 浏览器主流程 | `frontend/`：`node scripts/acceptance-shots.mjs`，连接临时数据根后端 | 素材保存、来源登记、来源命题回应、采用后个人命题创建通过；另测素材详情刷新通过 |
| 视觉核对 | 合成桌面 1440px、手机 390px 截图 | 已检查基础布局；截图为本地忽略产物，不作为远程交接的必需文件 |
| 问题复现 | 额外浏览器交互，等待请求及页面更新后检查 | FE-001、FE-002、FE-003 均复现 |
| 数据边界 | 项目根：`python scripts/check_data_boundary.py` | 上轮验收通过；每次提交在暂存后再运行 |

测试通过只说明现有用例覆盖的行为。修改代码后运行受影响检查；修复已知问题必须新增对应回归用例，不能只复述旧的通过数字。

浏览器验收脚本会写入合成记录，运行前必须确认后端使用系统临时数据根，并清除可能指向个人库的 `ARRIVE_DATABASE_URL`。不要直接对个人服务运行脚本。关闭自己启动的验收服务，保留其他任务的进程。

## 5. 每次交接如何更新

- 分别更新“前端进度”和“后端进度”，未涉及的一侧写“本轮无变化”。
- 新问题按 `FE-NNN` / `BE-NNN` 递增，注明发现时间、复现、影响、证据及验收条件；这是工程问题编号，与个人素材编号无关。
- 已修复问题保留编号并标记解决日期、提交和回归测试；未验证的修复不得标为完成。
- 记录本轮执行命令、结果、未测范围、是否触及 schema/迁移、其他工作区改动和下一位最先应做的事项。
- 更新交接历史，使用带时区时间。提交 hash 在生成后可在后续记录补充；不能凭空预填。远程状态以 Git 实际查询为准。
- 只提交软件、通用文档和合成测试；正文、来源、数据库、真实截图、日志与密钥继续留在数据根或明确的受控位置，不使用 `git add .` 混入本地材料。

### 交接记录模板

```text
时间（带时区）：
本轮目的 / 执行者（已知时填写）：
前端：完成内容；变更文件；新增/解决/遗留问题编号
后端：完成内容；API/schema/迁移变化；新增/解决/遗留问题编号
验证：工作目录、命令、结果；未验证范围
提交 / 远程同步：已知提交；实际推送结果（未完成写待确认）
工作区：本轮未纳入的其他文件/任务，不包含个人内容
下一位：优先任务、依赖、必要注意事项
```

## 6. 交接历史

### 2026-09-08T21:34:27+08:00 — 提交并同步近期实现

- 前端：本轮无业务变化；将本地数据库概览、旧档案页面、相关测试及 Docker 前端封装纳入提交。
- 后端：本轮无业务变化；将旧文件适配、数据库迁移、相关测试与 Docker 配置、依赖锁和运行指南纳入提交。
- 验证：沿用上一轮后端 55 项、前端 29 项测试及构建结果；提交前重新检查暂存范围和数据边界。Docker 运行验收仍待可用引擎或远程 CI 验证，BE-008 保留。
- 同步：已确认本地 main 与 origin/main 起点一致；本条随实现提交，推送结果以远程分支实际状态为准。
- 未纳入：个人数据、数据库和备份、本地报告及生成工具、根目录 Node 依赖和本地工具配置。下一位继续按既有工程问题顺序推进。

### 2026-09-08T21:08:26+08:00 — 本地数据库读取与旧文件适配

- 前端：首页新增真实数据库概览，页面打开与保存后读取；新增“本地档案”导航和只读原件展开，原件按需加载并以纯文本呈现。刷新页面仍可看到数据库记录。FE-001 至 FE-003 仍未修复。
- 后端：新增追加式 `legacy_documents` 表及 Alembic `b72e9104c301`；启动迁移后自动适配本地旧 Markdown。来源字段别名、明确来源命题与快照附件引用可适配；保留编号、原文件、ISO 原表示及入库时间。重复文件跳过，冲突不覆盖。新增两个只读档案接口。固定旧库 bootstrap 的第二版 revision，避免直接盖章未来 head 导致漏迁移；BE-006 的更广泛异常旧库检测仍待完善。
- 验证：后端 55 项测试通过，包含重复导入、文件变更、编号保留、快照、字段失败、路径逃逸、领域记录不误造、只读接口及三种迁移路径；前端 29 项测试与类型/构建通过，新增刷新后历史数据与原件延迟加载/HTML 转义验证。真实本地服务已备份数据库、重启并完成适配，通过只读接口和浏览器核对首页、档案列表。没有向真实库写入合成数据。
- BE-009：旧文件仅存磁盘而不在数据库的问题，在当前来源卡格式及只读档案范围内已解决；通用个人素材/回应/语义历史转换、运行期间自动监视、大库分页、非 UTF-8 跳过报告和冲突处理界面仍未实现，详见《本地数据库与旧文件适配》。本轮不宣称镜像或完整来源快照领域模型已实现。
- Docker：PyYAML 显式依赖与 Linux 哈希锁同步更新；BE-008 的容器运行验收仍受本机引擎不可用限制。
- 工作区 / 远程：本轮修改和上一轮 Docker 工作均未提交或推送；根目录报告、文档工具依赖和本地工具配置保持原状。所有真实内容、档案与备份仍留在忽略的数据根。
- 下一位：先按现有顺序修复草稿丢失，再推进 BE-001/BE-002；扩展适配器时保持追加、归属与时间语义，使用完全虚构测试。不要把旧档案直接当作已确认素材或个人立场。

### 2026-09-06T14:38:11+08:00 — 建立统一交接入口

- 前端：第一期实现基线为 `5c99b43`；完成真实浏览器验收，记录 FE-001 至 FE-003，尚未修复。临时设计指导书与模型开工指令已删除，运行说明保留在前端 README。
- 后端：整理项目内 Git 忽略数据根调整、启动 Alembic 迁移与旧库兼容改动，新增迁移测试随软件提交。未新增镜像或关键词接口。
- 验证：本日验收结果见第 4 节。本轮主要更新文档与引用，不以重复测试冒充问题修复。
- 提交范围：前端既有提交，加上本地后端/规范改动、验收记录与本交接文档。本条写入时新提交尚未生成；最终提交与同步结果以 Git 为准。
- 未纳入：根目录调研报告及其生成工具、根 Node 文档工具依赖和本地工具配置；这些不属于本次前后端交接提交。个人数据始终不入 Git。
- 下一位：优先修复 FE-001 至 FE-003 并补回归测试；后端按 BE-001/BE-002 推进，不因测试总数通过而将编码闭环标为完成。

### 2026-09-06T14:45:19+08:00 — README 技术展示

- 前端：根据实际 package.json、锁文件和 strict 配置添加 React、Vite、TypeScript 徽章及技术栈说明；业务代码无变化，FE-001 至 FE-003 仍待修。
- 后端：根据 pyproject.toml 添加 Python、FastAPI、SQLAlchemy、SQLite 徽章及依赖用途说明；业务代码、API 与迁移无变化。
- 验证：核对徽章工作流为已有 data-boundary.yml；明确该状态不等于完整测试 CI。仅文档变更，检查 Markdown 差异和数据边界，不重复运行业务测试。
- 提交 / 远程：上一轮进展已推送至 e6a9729；本轮 README 与交接更新尚未提交或推送。
- 下一位：继续优先处理三处前端草稿问题，技术徽章按实际配置维护。

### 2026-09-08T20:36:47+08:00 — 前后端 Docker 封装

- 前端：新增 Dockerfile、Nginx 配置和构建上下文白名单；运行镜像不包含 Node 开发服务器。FE-001 至 FE-003 本轮未修。
- 后端：固定 Python 镜像摘要，新增 Linux Python 3.12 运行/构建依赖哈希锁；安装不重新解析依赖，保留启动 Alembic 迁移。Compose 内后端不映射宿主机端口，数据默认绑定到项目 arrive-data。
- 交付：新增 Docker 运行指南、独立临时数据根验收脚本、Docker integration 工作流；本轮未改 API/schema，不需要新迁移。
- 验证：Compose config、前端类型与构建、后端 51 项测试通过；镜像摘要已从 registry 核对，锁文件已解析。`docker compose build` 因本机 Docker Desktop Linux engine 管道不存在而失败，启动引擎尝试未能恢复；Docker 运行与 CI 均未验证。
- 下一位：先在可用引擎运行容器验收并更新 BE-008，再视结果修正封装；旧命名卷使用者按指南备份迁移，不要删除旧数据。现有前端问题与后端领域缺口仍保留。
- 提交 / 远程：本轮尚未提交或推送，不代表已经存在远程镜像。

### 2026-09-09T09:56:00+08:00 — 内置 Docling 外部作品归档

- 前端：本轮未改动。文件上传、抓取按钮、提取结果显示与全文搜索尚未接入；FE-001 至 FE-003 不受本轮影响。
- 后端：新增独立本地 CLI 和 `documents` 可选依赖（Docling 2.126.0、ONNX Runtime）。先保存不可变原件，再追加 Markdown/JSON 提取及状态记录；记录来源快照、时间、哈希、工具版本与配置，转换不生成个人观点。无数据库模型/API 改动，无需迁移。
- 验证：后端全套 59 项通过后，新增超限/部分转换用例，归档模块 5 项通过。实际中文 HTML/表格与 DOCX 转换成功；PDF 的解析器路径问题通过更换后端推进到模型加载，但被本机 DLL 策略阻塞（BE-010），不得标为 PDF/OCR 完成。数据边界检查通过。测试与实转均使用合成文档和系统临时数据根。
- 文档：根 README、后端 README 和《外部作品归档》说明已更新；明确网页仅保存响应，不执行脚本、不递归保存资源，内容仍待人工核对。项目使用 Docling MIT 代码，模型/依赖另遵其许可。
- 下一步：在可加载依赖的环境验收 PDF/扫描件，补充复杂排版准确性验证，再对接来源数据库与前端；当前归档 CLI 需要明确指定来源编号与新快照版本。尚未建立文档可选依赖的完整跨平台哈希锁。
- 工作区：本轮未提交或推送；既有研究文档修改、个人数据与其他未跟踪文件保持原状。


### 2026-09-09T12:36:00+08:00 — 转换改为后端自动录入步骤

- 前端：没有新增转换按钮或 Docling 界面；仅同步来源 URL 可空类型，并在文件来源详情显示未提供链接。类型检查与构建通过。FE-001 至 FE-003 未修。
- 后端：新增 `document_jobs` 持久化队列，素材/来源创建与任务同事务提交；文件原始请求体先写入数据根并校验哈希，再登记来源。服务启动自动处理，工作线程文件锁排斥同一数据根的第二进程，中断任务重启恢复；原件及转换运行记录保留，失败不删除录入。
- 适配：直接调用 `docling-slim` 的转换接口及所需格式依赖，不启动上游服务、UI 或 CLI；直接文本保存 Markdown。提供文件录入、处理状态及 Markdown 读取接口，不提供用户执行转换的接口。默认安装和 Linux Python 3.12 哈希锁同步更新，Docker 补充 OCR 原生库；容器构建仍未验证。
- 迁移：`c09d20260909` 新建任务表，允许本地来源 URL 为空；旧来源不因升级自动抓取。迁移测试检查既有来源、作者命题和引用关系保留。仍未实现完整领域镜像/语义闭环，不宣称解决整个 BE-002。
- 验证：后端 66 项通过后追加带来源数据的迁移用例；新增自动录入回归 6 项通过。独立精简依赖环境中实际跑通“文件 API → 持久化队列 → 自动 Docling HTML 转换 → Markdown API”；没有手动调用转换步骤。此前 PDF/OCR DLL 环境阻塞仍在，未绕过系统策略。
- 编号校正：此前 PDF 环境问题误用了已用于旧文件适配的 BE-009，本轮将 PDF 问题及相关引用纠正为 BE-010；BE-009 继续保留原含义。
- 已知限制：失败任务尚无自动退避重试；后台按单进程工作，当前来源抓取不是完整网页镜像；纯 UTF-8 文本之外的编码不会猜测。自动任务已接入新录入，旧来源/失败档案不会批量重处理。
- 下一步：在依赖可正常加载的环境验证 PDF/OCR，随后验证复杂文档保真；进一步完善失败任务恢复策略、模型离线预取和受控网络隔离。当前无新增转换按钮需求。
- 本轮未提交或推送，保留既有工作区修改，真实数据未用于夹具或交接。

### 2026-09-09T12:39:47.9653457+08:00 — 自动录入最终回归

- 后端：完整测试 67 项通过（2 条现有依赖弃用提示）；覆盖新迁移、自动入队/处理、失败原件保留和中断恢复。独立精简依赖环境的自动 HTML API 全链路已通过；PDF/OCR 的 BE-010 限制仍保留。
- 前端：仅本地来源无 URL 的兼容显示，类型检查和构建通过，没有新增转换按钮。
- 文档与边界：README/归档指南已统一为默认自动转换；数据边界检查及差异空白检查通过。未提交、未推送，容器构建仍未验证。

### 2026-09-09T14:55:09+08:00 — PDF/OCR 本机恢复与实际验收

- 诊断：已初始化 Computer Use 并读取其限制，未自动操作 Windows 安全应用。代码完整性日志确认历史故障为 VerifiedAndReputableDesktop 策略拦截 SciPy 二进制模块；文件无数字签名。
- 后端：同一 SciPy 模块在原环境与独立精简环境重新导入均成功；未改安全设置、未重签/修改 DLL、未更换 SciPy 版本，故不将恢复原因归因于某项修复。Docling 本地模型完成加载。
- 验证：新增合成验收脚本，经文件录入 API 自动入队、后台转换、Markdown API 读取，文字 PDF 和中文扫描 PDF 均通过；核对目标文字、页码定位和原件完整性。脚本数据只在系统临时目录，无真实用户素材。已有业务代码本轮未修改，未重复完整单元测试。
- 前端：本轮无修改；无新增转换按钮。
- 进度：BE-010 更新为基础样本本次复测通过，历史失败记录保留。README 和归档指南同步；复杂版面与 OCR 泛化准确性不以两个样本替代专项评估。未使用重置卡。
- 下一步：后续依赖更新时运行该脚本；若再次拦截，先读代码完整性日志，不能通过关闭防护或篡改 DLL 宣称修复。容器验收仍待进行。

### 2026-09-09T20:21:52+08:00 — Windows 双击启动器

- 交付：根目录 `启动Arrive.cmd` 启动 `scripts/launch_arrive.py`，显示小型 Tk 窗口；后台启动项目虚拟环境的后端和本地 Vite，健康检查通过后打开浏览器。关闭窗口或点击停止会终止本启动器持有的进程树，不依据旧 PID 文件或按端口结束其他程序。
- 前端：业务界面无变化，启动器使用已安装的 Vite；已验证返回项目页面后才打开浏览器。
- 后端：业务 API/数据库模型无变化；直接启动已安装的 Uvicorn，使用既有数据根和自动转换工作线程。启动日志只写入数据根。双重启动由文件锁拒绝，端口占用时提示。
- 验证：系统临时数据根的实际前后端启动通过、重复启动拒绝通过、停止后 8000/5173 均释放；Tk 窗口初始化通过。测试没有写入个人数据库。无新增迁移。
- 文档：README 加入双击启动说明；数据边界白名单明确允许这个软件启动入口。新电脑仍需先安装依赖，当前机器已经就绪。未提交或推送。
